#!/bin/bash

source "$HOME/.recon-scripts/vars.sh"

target_name=$(basename "$1")
echo -e "$Run Fetching JS files of $target_name using hakrawler..."
cat "$1/httpx.txt" | hakrawler -plain -js -insecure -depth 1 > "$1/new_js.txt"
handle_diff $1 "js"
